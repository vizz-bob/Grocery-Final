import 'package:flutter/material.dart';

class BhejduColors {
  // Primary Brand Colors
  static const Color primaryBlue = Color(0xFF0A5DB6);
  static const Color primaryBlueLight = Color(0xFFE6F0FB); // AppBar bg tint
  static const Color iconBlue = Color(0xFF8FB8E8);

  // Background
  static const Color bgLight = Color(0xFFF5F7FA);
  static const Color white = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textDark = Color(0xFF1A1D26);
  static const Color textGrey = Color(0xFF7A7E89);

  /// ⭐ NEW → Required by your widgets (ProductCard, CategoryTiles, Home UI)
  static const Color textSecondary = Color(0xFF7A7E89); // same as textGrey
  static const Color skyBlue = Color(0xFFE3F2FD);       // soft light blue bg
  static const Color offerGreen = Color(0xFF43C268);    // bright green badge

  // Status / Success Colors
  static const Color successGreen = Color(0xFF43C268);
  static const Color successGreenLight = Color(0xFFE3F7EB);

  // Offer Colors
  static const Color offerYellow = Color(0xFFFFB000);
  static const Color offerOrange = Color(0xFFFF7A00);
  static const Color offerBlue = Color(0xFF005ED3);

  // Border / Shadow
  static const Color borderLight = Color(0xFFE1E5EB);
}
